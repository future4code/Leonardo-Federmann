export type authenticationData = {
    id: string,
    role: string
}

export type recipe = {
    id: string,
    name: string,
    description: string,
    createdAt: string,
    creator: string,
    creatorId: string
}